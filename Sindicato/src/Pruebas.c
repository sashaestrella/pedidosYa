//crearRestaurante nombre 5 [4,10] [Milanesas] [Milanesas,Empanadas,Ensaladas] [200,50,150] 2
//crearRestaurante nombre1 5 [4,10] [Milanesas] [Milanesas,Empanadas,Ensaladas] [200,50,150] 2
//crearRestaurante nombre2 5 [4,10] [Milanesas] [Milanesas,Empanadas,Ensaladas] [200,50,150] 2
//crearRestaurante nombre3 5 [4,10] [Milanesas] [Milanesas,Empanadas,Ensaladas] [200,50,150] 2
//crearReceta Milanesa [Reposar,Hornear] [4,6]




/*
        msg_guardar_pedido*mensaje=malloc(sizeof(msg_guardar_pedido));
		mensaje->id_pedido=1;
		mensaje->restaurante.largo_nombre=strlen("nombre3")+1;
		mensaje->restaurante.nombre=malloc(mensaje->restaurante.largo_nombre);
		strcpy(mensaje->restaurante.nombre,"nombre3");
		strcat(mensaje->restaurante.nombre,"\0");
		crearPedido((void*)mensaje);
		msg_guardar_plato*mensaje2=malloc(sizeof(msg_guardar_plato));
		mensaje2->cantidad_platos=5;
		mensaje2->id_pedido=1;
		mensaje2->plato.largo_nombre=strlen("pollo")+1;
		mensaje2->plato.nombre=malloc(mensaje2->plato.largo_nombre);
		strcpy(mensaje2->plato.nombre,"pollo\0");
		mensaje2->restaurante.largo_nombre=strlen("nombre3")+1;
		mensaje2->restaurante.nombre=malloc(mensaje2->restaurante.largo_nombre);
		strcpy(mensaje2->restaurante.nombre,"nombre3\0");
		guardarPlato((void*)mensaje2);
		guardarPlato((void*)mensaje2);
		free(mensaje2->restaurante.nombre);
		free(mensaje2->plato.nombre);
		free(mensaje2);
		msg_guardar_plato*mensaje3=malloc(sizeof(msg_guardar_plato));
		mensaje3->cantidad_platos=25;
		mensaje3->id_pedido=1;
		mensaje3->plato.largo_nombre=strlen("Milanesa")+1;
		mensaje3->plato.nombre=malloc(mensaje3->plato.largo_nombre);
		strcpy(mensaje3->plato.nombre,"Milanesa\0");
		mensaje3->restaurante.largo_nombre=strlen("nombre3")+1;
		mensaje3->restaurante.nombre=malloc(mensaje2->restaurante.largo_nombre);
		strcpy(mensaje3->restaurante.nombre,"nombre3\0");
		guardarPlato((void*)mensaje3);
		msg_confirmar_pedido*mensaje4=malloc(sizeof(msg_confirmar_pedido));
		mensaje4->id_pedido=1;
		mensaje4->nombre_restaurante.largo_nombre=strlen("nombre3")+1;
		mensaje4->nombre_restaurante.nombre=malloc(mensaje4->nombre_restaurante.largo_nombre);
		strcpy(mensaje4->nombre_restaurante.nombre,"nombre3\0");
		confirmarPedido((void*)mensaje4);
*/
